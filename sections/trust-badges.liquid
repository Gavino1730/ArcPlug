<style>
  .trust-badges {
    padding: 32px 0 24px 0;
    border-top: 1px solid rgba(var(--color-foreground), 0.12);
    border-bottom: 1px solid rgba(var(--color-foreground), 0.12);
  }

  .trust-badges__wrapper {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2.5rem;
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .trust-badge {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 0.75rem;
  }

  .trust-badge__icon {
    width: 4rem;
    height: 4rem;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.2rem;
  }

  .trust-badge__title {
    font-weight: 600;
    font-size: 1.05rem;
    color: rgba(var(--color-foreground), 0.9);
    margin: 0;
    line-height: 1.3;
  }

  .trust-badge__description {
    font-size: 0.875rem;
    color: rgba(var(--color-foreground), 0.7);
    margin: 0;
  }

  @media screen and (max-width: 749px) {
    .trust-badges {
      padding: 18px 0;
      border-top: none;
      border-bottom: none;
    }

    .trust-badges__wrapper {
      grid-template-columns: repeat(2, 1fr);
      gap: 1.25rem;
      padding: 0 1rem;
    }

    .trust-badge__icon {
      width: 3rem;
      height: 3rem;
      font-size: 1.65rem;
    }

    .trust-badge__title {
      font-size: 0.9rem;
    }
  }
</style>

<div class="trust-badges color-{{ section.settings.color_scheme }} gradient">
  <div class="trust-badges__wrapper">
    {% for block in section.blocks %}
      <div class="trust-badge" {{ block.shopify_attributes }}>
        <div class="trust-badge__icon">
          {% case block.settings.icon %}
            {% when 'truck' %}
              ✈️
            {% when 'shield' %}
              ✓
            {% when 'refresh' %}
              ↻
            {% when 'lock' %}
              🔒
            {% else %}
              ✓
          {% endcase %}
        </div>
        <h3 class="trust-badge__title">{{ block.settings.title }}</h3>
      </div>
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "Trust Badges",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color Scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 40
    }
  ],
  "blocks": [
    {
      "type": "trust_badge",
      "name": "Badge",
      "limit": 4,
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Badge Title"
        },
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            {
              "value": "truck",
              "label": "Fast Shipping"
            },
            {
              "value": "shield",
              "label": "Weather Tested"
            },
            {
              "value": "refresh",
              "label": "Free Returns"
            },
            {
              "value": "lock",
              "label": "Secure Checkout"
            }
          ],
          "default": "truck"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Trust Badges",
      "blocks": [
        {
          "type": "trust_badge",
          "settings": {
            "title": "Free Shipping Always",
            "icon": "truck"
          }
        },
        {
          "type": "trust_badge",
          "settings": {
            "title": "Weather Tested Gear",
            "icon": "shield"
          }
        },
        {
          "type": "trust_badge",
          "settings": {
            "title": "14‑Day Returns",
            "icon": "refresh"
          }
        },
        {
          "type": "trust_badge",
          "settings": {
            "title": "Secure Payments",
            "icon": "lock"
          }
        }
      ]
    }
  ]
}
{% endschema %}
